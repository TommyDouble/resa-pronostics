DATABASE_URL=./resa.db
SECRET_KEY=replace-with-a-generated-secret
BASE_URL=http://localhost:8000
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=pronos@resa.be
EMAIL_WEBHOOK_URL=
EMAIL_WEBHOOK_SECRET=
EMAIL_SENDER_NAME=RESA Pronostics 2026

# Rappels automatiques (1 = activés)
SCHEDULER_ENABLED=1
SCHEDULER_INTERVAL=600

# Notifications push web — générer avec: python scripts/generate_vapid_keys.py
# Laisser vide pour désactiver le push (les rappels partent alors par email).
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_CLAIMS_EMAIL=pronos@resa.be
